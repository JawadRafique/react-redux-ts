export interface Types {
    user: {
        id: number;
        name: string;
        username: string;
        phone: number;
    };
}

export interface User {
    id: number;
    name: string;
    username: string;
    phone: number;
}
